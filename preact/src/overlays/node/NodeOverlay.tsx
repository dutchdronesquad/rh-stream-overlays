import type { OverlayRuntimeConfig } from "../../core/overlayRuntime";

type Props = {
  runtime: OverlayRuntimeConfig;
};

// Fase 3: implement node overlay.
export function NodeOverlay(_props: Props) {
  return <></>;
}
